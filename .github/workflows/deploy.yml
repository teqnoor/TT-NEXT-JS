name: 🚀 Tiger Tiger FTP Deploy

on:
  push:
    branches: ["main"]

jobs:
  build-and-deploy:
    name: 🎉 Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repo
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🏗 Build Project
        run: npm run build

      - name: 📂 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.2
        with:
          server: 153.92.6.140
          username: u405465134.tt
          password: b;L042J=YguhVuCm
          port: 21
          protocol: ftp
          local-dir: .next/ # Use out/ if using next export
          server-dir: / # Change if needed
          timeout: 999999
